using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WaterController : MonoBehaviour {


	// NOT USED AS CAUSES SEA_SICKNESS


	/*
	public GameObject followObject;

	private Vector3 objectOffset;

	void Start () {

		objectOffset = followObject.transform.position - this.transform.position;
	
	}
	
	// Update is called once per frame
	void Update () {


		this.transform.position = new Vector3 (objectOffset.x + followObject.transform.position.x, 
			this.transform.position.y, 
			objectOffset.z + followObject.transform.position.z);

	}

	*/
}
